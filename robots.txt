# Nothing is disallowed on purpose. The site inherited a stale index from the
# WordPress build it replaces, and Google can only drop those URLs by crawling
# them and seeing the 404 / redirect. Blocking them here would freeze them in
# the results instead. See documentation/README.md.
User-agent: *
Allow: /

Sitemap: https://www.lste.lu/sitemap.xml
